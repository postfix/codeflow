> **User-directed amendment — 2026-09-12:** Do not execute the original Slice 1 row below.
> Slice 1 is complete from the observed native Codex nested run using the verified
> `chatgpt-subscription` route with no API fallback. Delete its disposable spike artifacts.
> Human consent remains in Slice 9; complete real Codex/Claude acceptance remains in Slice 11.
>
> **Slice 3 scope correction — 2026-09-13:** `src/journal.ts` is also in Slice 3's exact
> files so fresh-process initialization can resume the existing verified hash chain. Reusing
> that journal is required; a second append implementation in the supervisor is not permitted.
>
> **Platform qualification correction — 2026-09-15:** WSL2 workspaces on their Linux
> filesystem belong to the Linux target class and do not require a duplicate WSL2 witness.
> macOS qualification runs on a GitHub-hosted macOS runner. Slice 6 CI evidence covers build,
> storage, process interruption, and fresh-process recovery; later adapter fixtures remain
> in Slice 11. The workflow does not
> claim a physical reboot or interactive native-provider login on that disposable runner.

| Slice | Outcome | Dependencies | Exact files | Direct witness | Test prerequisites | Temporary artifacts | Visible review triggers |
|---|---|---|---|---|---|---|---|
| 1 | Native-host feasibility tracer: both real hosts use their ordinary subscription route for a durable, multi-process nested task; the person's rejection causes zero edits, the person's approval causes exactly one edit, and a fresh process reconnects to the same pending request. Stop the build if either host fails. | none | `.agents/skills/codeflow-feasibility/SKILL.md`<br>`.claude/skills/codeflow-feasibility/SKILL.md`<br>`spikes/native-host/probe.mjs`<br>`spikes/native-host/result.schema.json`<br>`spikes/native-host/fixture/target.txt`<br>`spikes/native-host/results/.gitkeep` | Manual host witness because only a real host can display evidence and relay the person's actual answer. Before each provider, run `node -e "require('node:fs').writeFileSync('spikes/native-host/fixture/target.txt','codeflow-native-host-probe\\n')"` and require exit `0`. In Codex invoke `$codeflow-feasibility`; in Claude invoke `/codeflow-feasibility`. The Codex wrapper runs, in order: `node spikes/native-host/probe.mjs start --provider codex --state spikes/native-host/results/codex-state.json --request spikes/native-host/results/codex-reject-request.json` expecting `10`; displays that request; writes `codex-reject-reply.json` as `{"submissionId":"<fresh UUID>","requestId":"<copy requestId>","revision":"<copy revision>","presentationHash":"<copy presentationHash>","answer":{"decision":"reject","reason":"<person's actual reason, or empty string>"}}`; runs `node spikes/native-host/probe.mjs respond --state spikes/native-host/results/codex-state.json --reply spikes/native-host/results/codex-reject-reply.json --next-request spikes/native-host/results/codex-approve-request.json` expecting `10`; displays that request; writes `codex-approve-reply.json` with copied identity fields, a fresh submission UUID, and `"answer":{"decision":"approve","reason":"<person's actual reason, or empty string>"}`; runs `node spikes/native-host/probe.mjs status --state spikes/native-host/results/codex-state.json` expecting `10` and the same `requestId`, `revision`, and `presentationHash`; then runs `node spikes/native-host/probe.mjs respond --state spikes/native-host/results/codex-state.json --reply spikes/native-host/results/codex-approve-reply.json --result spikes/native-host/results/codex.json` expecting `0`. The Claude wrapper runs, in order: `node spikes/native-host/probe.mjs start --provider claude --state spikes/native-host/results/claude-state.json --request spikes/native-host/results/claude-reject-request.json` expecting `10`; displays the request and writes `claude-reject-reply.json` with the same exact five reply fields and `"answer":{"decision":"reject","reason":"<person's actual reason, or empty string>"}`; runs `node spikes/native-host/probe.mjs respond --state spikes/native-host/results/claude-state.json --reply spikes/native-host/results/claude-reject-reply.json --next-request spikes/native-host/results/claude-approve-request.json` expecting `10`; displays the request and writes `claude-approve-reply.json` with copied identity fields, a fresh submission UUID, and `"answer":{"decision":"approve","reason":"<person's actual reason, or empty string>"}`; runs `node spikes/native-host/probe.mjs status --state spikes/native-host/results/claude-state.json` expecting `10` and the same pending identity; then runs `node spikes/native-host/probe.mjs respond --state spikes/native-host/results/claude-state.json --reply spikes/native-host/results/claude-approve-reply.json --result spikes/native-host/results/claude.json` expecting `0`. Finally `node spikes/native-host/probe.mjs verify --codex-result spikes/native-host/results/codex.json --claude-result spikes/native-host/results/claude.json` must exit `0` and report schema-valid exact host/provider versions, recognized subscription routes, distinct nested run identities, structured-result hashes, before hash for exact bytes `codeflow-native-host-probe\n`, after hash for exact bytes `codeflow-native-host-probe: approved\n`, both request/reply identities, rejected effect count `0`, approved effect count `1`, and reconnection `true`. | Activate `/home/john/.nvm/versions/node/v24.19.0`; `node` v24.19.0; `codex` v0.154.0; `claude` v2.1.266; both native subscription logins; actual human replies; writable disposable fixture. | Generated state, request, reply, raw-output, and result files under `spikes/native-host/results/`; reset fixture bytes before each provider. | QA adversarial; Code Review; Security audit (native auth, permissions, actual-human relay, secret-free artifacts); QA verification. |
| 2 | Minimal durable pure-flow success: a workspace TypeScript entry validates input, compiles once, starts a detached supervisor that owns the stable lock, durably records `run.created` before returning its run ID, and commits validated output retrievable through the host API. | Slice 1 | `package.json`<br>`package-lock.json`<br>`tsconfig.json`<br>`eslint.config.js`<br>`.gitignore`<br>`src/sdk.ts`<br>`src/host.ts`<br>`src/contracts.ts`<br>`src/build.ts`<br>`src/artifacts.ts`<br>`src/journal.ts`<br>`src/worker.ts`<br>`src/supervisor.ts`<br>`tests/execution-replay.test.ts`<br>`tests/fixtures/host-flow.ts` | `npm run typecheck && npm test -- --run tests/execution-replay.test.ts -t "minimal durable pure flow"` must exit `0`, prove invalid input executes zero flow-body effects, prove the durable `run.created` event exists before the returned run ID, kill the launcher after handshake while the supervisor retains its lock, and retrieve one validated terminal output from a fresh host handle. Manual analyzer fallback: durable startup and process lifetime are cross-process behaviors rather than a source-local ordered obligation. | Node v24.19.0; `npm`; dependency installation from the committed lockfile; local compiler toolchain for the locked native dependency. | Test workspaces and run stores under the test runner's temporary directory. | QA adversarial; Code Review; Security audit (input/build containment, supervisor lock ownership, durable run identity); QA verification. |
| 3 | Initialization-resume and public contracts: a crash after durable creation resumes initialization from the pinned bundle and original input without refunding charges; public exports infer valid use and reject invalid or nonportable contracts. | Slice 2 | `src/testing.ts`<br>`src/sdk.ts`<br>`src/host.ts`<br>`src/contracts.ts`<br>`src/build.ts`<br>`src/worker.ts`<br>`src/supervisor.ts`<br>`tests/contracts.test.ts`<br>`tests/execution-replay.test.ts`<br>`tests/types/positive.ts`<br>`tests/types/negative.ts` | `npm run typecheck && npm test -- --run tests/contracts.test.ts tests/execution-replay.test.ts -t "initialization and contract"` must exit `0`, compile the positive fixture, reject every expected negative fixture, reject non-JSON/nonportable/oversized input before author work, crash after `run.created`, then resume from a fresh process with the original bundle/input identity and unchanged cumulative initialization charges. Manual analyzer fallback: compiler behavior and cross-process initialization recovery are not one call-order pattern. | Node v24.19.0; `npm`; dependencies from the committed lockfile. | Generated bundles, type fixtures, crash markers, and run stores in test-owned temporary directories. | QA adversarial; Code Review; Security audit (contract boundary, pinned bundle/input identity, crash recovery); QA verification. |
| 4 | Durable evidence and workspace freshness: prompts and canonical values are bounded; immutable artifacts are synchronized before referencing events; verified hash-linked history and whole-workspace baselines block torn, corrupt, escaping, unstable, or stale evidence. | Slices 2–3 | `src/prompts.ts`<br>`src/workspace.ts`<br>`src/contracts.ts`<br>`src/artifacts.ts`<br>`src/journal.ts`<br>`src/supervisor.ts`<br>`tests/storage.test.ts`<br>`tests/workspace.test.ts` | Run `sf-verify-cert verdict /home/john/go/src/github.com/postfix/codeflow/.smtc/analyzers/codeflow-artifact-before-event.yaml` against unsafe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/unsafe`, safe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/safe`, and product root `/home/john/go/src/github.com/postfix/codeflow/src`; require exactly one unsafe finding, zero safe findings with non-vacuous matches, zero product findings, and complete analysis. Then `npm run typecheck && npm test -- --run tests/storage.test.ts tests/workspace.test.ts` must exit `0` for short writes, sync/rename/append failures, corruption, bounds, symlinks, unstable scans, and stale workspace. | Node v24.19.0; `npm`; `smtc`; retained durable analyzer controls; dependencies from the committed lockfile. | Fault-injection stores, corrupt journals, bounded large-file fixtures, and workspace copies in test-owned temporary directories. | QA adversarial; Code Review; Security audit (path traversal, symlinks, artifact containment, corruption, private modes); QA verification. |
| 5 | Owned command effect: the sole supervisor registers an inert runner, durably authorizes one literal argv operation, launches it once in an owned process group, captures bounded return evidence, proves the group absent immediately before outcome commit, and never signals a reused identity. | Slices 2–4 | `src/ownership.ts`<br>`src/process.ts`<br>`src/runner.ts`<br>`src/host.ts`<br>`src/testing.ts`<br>`src/supervisor.ts`<br>`src/journal.ts`<br>`tests/ownership-process.test.ts`<br>`tests/crash-matrix.test.ts`<br>`tests/fixtures/process-child.mjs` | Run `sf-verify-cert verdict /home/john/go/src/github.com/postfix/codeflow/.smtc/analyzers/codeflow-authorization-before-execution.yaml` and `sf-verify-cert verdict /home/john/go/src/github.com/postfix/codeflow/.smtc/analyzers/codeflow-cleanup-before-commit.yaml` against the retained unsafe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/unsafe`, safe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/safe`, and product root `/home/john/go/src/github.com/postfix/codeflow/src`; each must return one/zero/zero findings with non-vacuous complete analysis. Then `npm run typecheck && npm test -- --run tests/ownership-process.test.ts tests/crash-matrix.test.ts -t "authorization ownership process group"` must exit `0` for pre-authorization effect count `0`, duplicate authorization effect count at most `1`, literal argv, concurrent ownership, launcher/runner/parent loss, descendants, cancellation, final absence, and reused-group safety. | Node v24.19.0 on the current Linux target; `npm`; `smtc`; buildable locked native dependency; retained durable analyzer controls; real POSIX signals and process groups. | Temporary process trees, logs, run stores, and reused-identity probes owned by the test harness. | QA adversarial; Code Review; Security audit (process authority, stale identity, command injection, cancellation); QA verification. |
| 6 | Foundation qualification for the two supported runtime classes: Linux proves native-addon, storage, process-interruption, and real-reboot recovery; macOS proves native-addon, storage, process-interruption, and fresh-process recovery on GitHub Actions before dependent work is accepted. WSL2 on its Linux filesystem is Linux for this qualification. | Slice 5 | `package.json`<br>`package-lock.json`<br>`.github/workflows/macos-foundation.yml`<br>`src/ownership.ts`<br>`src/process.ts`<br>`src/artifacts.ts`<br>`src/journal.ts`<br>`src/testing.ts`<br>`tests/qualification.test.ts`<br>`tests/storage.test.ts`<br>`tests/ownership-process.test.ts`<br>`tests/fixtures/process-child.mjs` | Linux witness: run `npm run typecheck`; run `npm run test:foundation -- --target linux-local --phase prepare --scenario process-interrupt --evidence .coding-flow-qualification/linux-local-process.json`; run the same command with `--phase interrupt`; from a fresh process run it with `--phase resume`; then run it with `--phase verify`. Run `npm run test:foundation -- --target linux-local --phase prepare --scenario reboot --evidence .coding-flow-qualification/linux-local-reboot.json`; reboot the disposable Linux machine; then run the same command with `--phase resume` and with `--phase verify`. Every command must exit `0`. The linux-local target accepts ordinary Linux and WSL2 only when the workspace filesystem is Linux-native; it records the observed kernel and filesystem but creates no separate WSL2 target. macOS witness: `.github/workflows/macos-foundation.yml` runs on macos-latest for the exact commit, installs Node v24.19.0 with locked dependencies, runs `npm run typecheck`, `npm test -- --run tests/storage.test.ts tests/ownership-process.test.ts tests/qualification.test.ts`, and the four process-interrupt commands above with target macos-github; every step must exit `0` and the uploaded macos-foundation artifact must contain a passing process record for the same commit. The macOS job records the exact runner image, OS, architecture, filesystem, Node, and addon identities and makes no reboot or interactive-provider claim. | Linux: Node v24.19.0; `npm`; compiler toolchain; locked native dependency; Linux-native workspace filesystem; real signals/process groups; permission to interrupt descendants and reboot a disposable machine. macOS: GitHub Actions enabled for the repository, a GitHub-hosted macos-latest runner, and permission to run the workflow and download its artifact. | Linux `.coding-flow-qualification/linux-local-process.json` and `linux-local-reboot.json`; GitHub Actions macos-foundation artifact containing `macos-github-process.json`; disposable qualification workspaces and processes. | QA adversarial; Code Review; local process-authority review; QA verification. |
| 7 | Replay frontier and cumulative admission: committed command and fake-provider outcomes reconstruct branches with zero redispatch; durable commit precedes worker delivery; workspace freshness and non-resetting limits precede every new attempt; mismatched identity, trace, history, schema, or evidence blocks. | Slice 6 qualified for the current development target | `src/replay.ts`<br>`src/worker.ts`<br>`src/supervisor.ts`<br>`src/host.ts`<br>`src/testing.ts`<br>`src/journal.ts`<br>`src/workspace.ts`<br>`tests/execution-replay.test.ts`<br>`tests/crash-matrix.test.ts` | Run `sf-verify-cert verdict` for `/home/john/go/src/github.com/postfix/codeflow/.smtc/analyzers/codeflow-commit-before-delivery.yaml`, `/home/john/go/src/github.com/postfix/codeflow/.smtc/analyzers/codeflow-freshness-before-prepare.yaml`, and `/home/john/go/src/github.com/postfix/codeflow/.smtc/analyzers/codeflow-limits-before-prepare.yaml`, each against unsafe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/unsafe`, safe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/safe`, and product root `/home/john/go/src/github.com/postfix/codeflow/src`; require one/zero/zero findings with non-vacuous complete analysis for each. Then `npm run typecheck && npm test -- --run tests/execution-replay.test.ts tests/crash-matrix.test.ts -t "replay frontier limits freshness"` must exit `0`, prove committed calls redispatch `0` times, budgets never reset, and every named mismatch blocks before a new effect. | Node v24.19.0; `npm`; `smtc`; retained durable analyzer controls; a passing Slice 6 foundation record for the current target; deterministic fake command/provider returns and fake clock. | Temporary bundles, histories, baselines, crash markers, and dispatch counters. | QA adversarial; Code Review; Security audit (replay confusion, stale evidence, budget reset, unknown outcomes); QA verification. |
| 8 | Complete author primitives: agent calls, commands, reads, phases, nested flows, prompts/instructions, serial async discipline, workspace updates, checks/logs, and terminal finalization work through the public interface without hidden concurrency or stale proof. | Slice 7 | `src/sdk.ts`<br>`src/contracts.ts`<br>`src/prompts.ts`<br>`src/build.ts`<br>`src/worker.ts`<br>`src/supervisor.ts`<br>`src/workspace.ts`<br>`src/testing.ts`<br>`tests/contracts.test.ts`<br>`tests/execution-replay.test.ts`<br>`tests/workspace.test.ts`<br>`tests/types/positive.ts`<br>`tests/types/negative.ts` | `npm run typecheck && npm run lint && npm test -- --run tests/contracts.test.ts tests/execution-replay.test.ts tests/workspace.test.ts -t "author primitive composition finalization"` must exit `0` for branching/composition, nested limits, prompt/JSON/IPC bounds, literal commands, reads, workspace mutation, pending/overlap/sticky errors, finalization, and public positive/negative type fixtures. Manual analyzer fallback: type inference, async discipline, bounded encoding, and workspace semantics require compiler/runtime witnesses. | Node v24.19.0; `npm`; dependencies from the committed lockfile; passing current-target foundation evidence. | Generated bundles, instruction snapshots, temporary workspaces, and typecheck diagnostics. | QA adversarial; Code Review; Security audit (instruction/path containment, async bypass, finalization); QA verification. |
| 9 | Human consent and unknown-effect recovery: immutable evidence precedes questions; validated historical deduplication precedes active-state use; reply decision, fresh single-use approval gate, author delivery, and first approved effect are separate durable boundaries; unresolved effects offer only evidence-eligible choices. | Slice 8 | `src/human.ts`<br>`src/recovery.ts`<br>`src/sdk.ts`<br>`src/host.ts`<br>`src/contracts.ts`<br>`src/artifacts.ts`<br>`src/journal.ts`<br>`src/workspace.ts`<br>`src/worker.ts`<br>`src/supervisor.ts`<br>`src/testing.ts`<br>`tests/human-recovery.test.ts`<br>`tests/crash-matrix.test.ts` | Run `sf-verify-cert verdict /home/john/go/src/github.com/postfix/codeflow/.smtc/analyzers/codeflow-validation-before-state-use.yaml` and `sf-verify-cert verdict /home/john/go/src/github.com/postfix/codeflow/.smtc/analyzers/codeflow-approval-before-effect.yaml` against unsafe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/unsafe`, safe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/safe`, and product root `/home/john/go/src/github.com/postfix/codeflow/src`; each must return one/zero/zero findings with non-vacuous complete analysis. Then `npm run typecheck && npm test -- --run tests/human-recovery.test.ts tests/crash-matrix.test.ts -t "human approval recovery"` must exit `0` for evidence-before-request, invalid/stale/rejected/expired replies, duplicate A after B/terminal/restart, every reply/gate/effect crash boundary, one-effect approval scope, eligible recovery choices, and zero unauthorized redispatch. | Node v24.19.0; `npm`; `smtc`; retained durable analyzer controls; passing current-target foundation evidence; fake clock and fresh real processes for crash cases. | Presentation copies, inbox envelopes, receipts, recovery evidence, crash-state workspaces, and test-owned process trees. | QA adversarial; Code Review; Security audit (consent identity/freshness, same-user forgery limit, stale evidence, recovery authority); QA verification. |
| 10 | Native adapters and lifecycle CLI: qualified Codex/Claude invocations use the same durable authorization path; strict decoders, lifecycle commands, collision-safe wrappers, discovery/reconnection, cancellation, and sanitized failures preserve native auth, model, and permissions. | Slices 1, 6–9 | `src/adapters/types.ts`<br>`src/adapters/codex.ts`<br>`src/adapters/claude.ts`<br>`src/cli.ts`<br>`src/install.ts`<br>`src/process.ts`<br>`src/supervisor.ts`<br>`src/host.ts`<br>`tests/adapters.test.ts`<br>`tests/cli-install.test.ts`<br>`tests/fixtures/codex.jsonl`<br>`tests/fixtures/claude.json`<br>`README.md`<br>`.gitignore` | Run `sf-verify-cert verdict /home/john/go/src/github.com/postfix/codeflow/.smtc/analyzers/codeflow-provider-preflight-before-dispatch.yaml` against unsafe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/unsafe`, safe root `/home/john/go/src/github.com/postfix/codeflow/.smtc/controls/codeflow/safe`, and product root `/home/john/go/src/github.com/postfix/codeflow/src`; require one/zero/zero findings with non-vacuous complete analysis. Then `npm run typecheck && npm run lint && npm test -- --run tests/adapters.test.ts tests/cli-install.test.ts` must exit `0` for exact argv, subscription-status conflicts, output bounds, terminal decoding, permission/model mismatch, strict JSON/flags/exit codes, wrapper collision, unresolved-run discovery, attach/resume, cancellation, and explicit pruning. | Node v24.19.0; `npm`; `smtc`; `codex` v0.154.0; `claude` v2.1.266; sanitized fixtures; retained durable analyzer controls; passing current-target foundation evidence. | Fresh adapter attempt directories, generated wrapper candidates, CLI JSON files, and captured sanitized outputs. | QA adversarial; Code Review; Security audit (auth route, native permission non-bypass, secret redaction, CLI/path injection); QA verification. |
| 11 | Live both-host acceptance on Linux: the same authored fixture flow completes through installed thin wrappers in real Codex and Claude hosts on ordinary Linux or WSL2 using its Linux filesystem, including structured output/edit, actual rejection and approval, reconnection, explicit crash recovery, auth expiry, quota failure, permission denial, and cancellation. The macOS GitHub job reruns strict adapter and lifecycle fixtures but makes no interactive native-login claim. | Slice 10 and passing Slice 6 Linux evidence | `tests/qualification.test.ts`<br>`tests/crash-matrix.test.ts`<br>`tests/fixtures/host-flow.ts`<br>`.github/workflows/macos-foundation.yml`<br>`README.md` | Manual Linux host witness because native subscription sessions and actual human decisions cannot be synthesized. First run `npm run typecheck`; install `tests/fixtures/host-flow.ts` for Codex and Claude with the built CLI; invoke the installed acceptance wrapper in each host; reject the first displayed approval, approve the revised request, close and reconnect the host context, interrupt the supervisor once, resume, and finish. Then run `npm run test:host-acceptance -- --target linux-local --provider codex --result .coding-flow-qualification/linux-local-codex-live.json` and the same command for Claude; each must exit `0` and report exact host/provider/runtime/model identity, recognized subscription route, matching fixture hash, structured output, expected edit hash, rejection effect count `0`, approval effect count `1`, reconnection, recovery without committed redispatch, preserved limits, and explicit auth-expiry/quota/permission/cancellation outcomes. The macOS GitHub workflow runs `npm test -- --run tests/adapters.test.ts tests/cli-install.test.ts` and must pass for the exact commit; this proves macOS fixture compatibility only. | Linux-native filesystem on ordinary Linux or WSL2; Node v24.19.0; `npm`; built package; exact installed `codex` and `claude`; both native subscription logins; actual human replies; disposable workspace; passing Linux foundation evidence. GitHub Actions enabled for the macOS fixture job. | Generated host wrappers and `.coding-flow/` run data in a disposable Linux workspace; `linux-local-codex-live.json` and `linux-local-claude-live.json`; sanitized provider output only. | QA adversarial; Code Review; local native-process and consent-boundary review; QA verification. |
| 12 | Final qualification and publication: one strict aggregator rejects missing, stale, mismatched, or failed evidence; the full deterministic and crash suite passes; Linux compatibility includes WSL2 workspaces on their Linux filesystem, while macOS compatibility is published with its narrower GitHub-CI evidence stated explicitly. | Slices 1–11; current Linux foundation/live-host evidence and current macOS GitHub Actions evidence | `compatibility.json`<br>`.github/workflows/macos-foundation.yml`<br>`tests/qualification.test.ts`<br>`tests/crash-matrix.test.ts`<br>`tests/storage.test.ts`<br>`tests/ownership-process.test.ts`<br>`tests/adapters.test.ts`<br>`tests/cli-install.test.ts`<br>`package-lock.json`<br>`README.md`<br>`docs/codebase-overview.md` | `npm run typecheck && npm run lint && npm test` must exit `0`. Then `npm run test:qualification -- --evidence .coding-flow-qualification --output compatibility.json` must exit `0` only when Linux has fresh passing process-interrupt, real-reboot, Codex-live, and Claude-live records, and the exact commit has a fresh passing macos-foundation GitHub Actions artifact covering typecheck, storage, process interruption/fresh-process recovery, qualification, adapter, and lifecycle fixtures. A negative control with any required record or GitHub artifact absent, stale, failed, or mismatched must exit nonzero and leave `compatibility.json` unchanged. The successful record lists exact engine, Node, Zod, esbuild, native-addon, provider, OS/filesystem/architecture identities, fixture hash, qualification date, and verification coverage per runtime class. README states that WSL2 on its Linux filesystem is covered by Linux, and that macOS has GitHub-CI coverage without a real-reboot or interactive native-provider acceptance claim. Manual analyzer fallback: release support is executed multi-environment evidence, not a source-local invariant. | Node v24.19.0; `npm`; complete fresh Slice 6 and Slice 11 Linux evidence; a successful exact-commit macOS GitHub Actions run and downloaded macos-foundation artifact; exact locked dependencies; both real Linux host subscription routes; disposable qualification environments. | Disposable negative-control evidence copies and generated compatibility candidate; downloaded sanitized macOS CI artifact; retain only approved sanitized evidence and final `compatibility.json`. | QA adversarial; Code Review; local evidence-integrity review; QA verification. |

## Red Team

- Initial Gate 4 verdict: `NEEDS REVISION`.
- Corrections: split the oversized happy-path increment; moved current-target foundation qualification before replay, human, and provider consumers; replaced ephemeral analyzer controls with tracked controls outside `src`; made both hosts' commands, exit codes, reset bytes, complete reply fields, and wrapper invocations explicit; split production qualification into per-target foundation evidence, live both-host acceptance, and strict final aggregation/publication; made all three required target classes mandatory.
- Threat model: `MODELED`; Gate 3's eight retained threats remain the security scope for implementation review.
- Final independent Gate 4 verdict: `SOUND`; findings: none. All eight lenses checked clean. Product source proof remains unavailable until implementation, and the native host, storage, process, reboot, and three-platform outcomes remain explicit slice obligations rather than claimed facts.
- 2026-09-15 platform amendment: the named `sf-red-team` worker was unavailable, so no independent amendment verdict is claimed. A focused Hand consistency check moved macOS adapter fixtures from Slice 6 to Slice 11, kept real reboot and live native-host evidence on Linux, grouped WSL2 Linux-filesystem workspaces into the Linux runtime class, and made macOS GitHub-CI limits explicit in Slices 6, 11, and 12.
